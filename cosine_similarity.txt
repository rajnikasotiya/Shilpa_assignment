# !pip install pandas nltk sentence-transformers faiss-cpu

import pandas as pd
import re
import string
import nltk
import numpy as np
import faiss
from nltk.corpus import stopwords
from nltk.stem import PorterStemmer
from sentence_transformers import SentenceTransformer

# Load dataset
df = pd.read_csv("medical.dat", sep="\t", names=["TEXT", "label"], encoding='utf-8')

# Download stopwords
nltk.download('stopwords')
stop_words = set(stopwords.words('english'))
stemmer = PorterStemmer()

# Preprocessing function
def preprocess(text):
    text = str(text).lower()
    text = re.sub(r'\d+', '', text)
    text = text.translate(str.maketrans('', '', string.punctuation))
    tokens = text.split()
    tokens = [stemmer.stem(tok) for tok in tokens if tok not in stop_words]
    return ' '.join(tokens)

# Apply preprocessing
df['clean_text'] = df['TEXT'].apply(preprocess)

# Sentence embeddings
model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2')
corpus = df['clean_text'].tolist()
corpus_embeddings = model.encode(corpus, convert_to_numpy=True).astype('float32')

# Normalize and index embeddings
faiss.normalize_L2(corpus_embeddings)
dim = corpus_embeddings.shape[1]
index = faiss.IndexFlatIP(dim)
index.add(corpus_embeddings)

# User input query
query = input("Enter your medical query: ")
query_processed = preprocess(query)
query_embedding = model.encode([query_processed], convert_to_numpy=True).astype('float32')
faiss.normalize_L2(query_embedding)

# Perform similarity search
top_k = 5
scores, indices = index.search(query_embedding, top_k)

# Display results
print(f"\nQuery: {query}\n\nTop {top_k} most similar results:\n")
for i in range(len(indices[0])):
    idx = indices[0][i]
    score = scores[0][i]
    print(f"Score: {score:.4f}")
    print(f"Label: {df.iloc[idx]['label']}")
    print(f"Text: {df.iloc[idx]['TEXT'][:300]}...\n")






query = "persistent cough and chest pain"
query = "severe headaches and blurry vision"
query = "kidney failure and dialysis treatment"
query = "abdominal pain and liver issues"
Absolutely! Here are some **example queries** you can try with your medical text similarity system:

---

### 🩺 **Symptom-based Queries**

1. `"abdominal pain and nausea"`
2. `"fever with chills and muscle aches"`
3. `"shortness of breath and chest discomfort"`
4. `"persistent dry cough and fatigue"`
5. `"blurred vision and dizziness"`

---

### 🧬 **Condition-based Queries**

6. `"symptoms of liver cirrhosis"`
7. `"early signs of diabetes"`
8. `"complications of high blood pressure"`
9. `"treatment options for asthma"`
10. `"how to manage chronic kidney disease"`

---

### 💊 **Treatment/Procedure Queries**

11. `"insulin therapy for type 2 diabetes"`
12. `"side effects of chemotherapy"`
13. `"dialysis process and patient care"`
14. `"rehabilitation after heart attack"`
15. `"surgical treatment for gallstones"`

---

### 👩‍⚕️ **Medical Advice Style Queries**

16. `"when to see a doctor for chest pain"`
17. `"what causes sudden weight loss"`
18. `"how to control cholesterol naturally"`
19. `"can migraines cause vision loss"`
20. `"how does smoking affect the lungs"`

---

### 💡 Tip:

Start with queries that match the tone or style of the records in your `medical.dat` dataset. If your dataset is more technical or symptom-focused, use shorter, precise queries.

---

